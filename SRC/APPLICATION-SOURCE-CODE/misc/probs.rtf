

to Zomer:

5. query 7 returns events


to SQL team:
1. reviews are ordered by descending 'updated_at' (timestamp) -> add timestamp attribute to reviews.
2. add query creates review with:
    artist_id, username, text, rank, timestamp(indexed?)